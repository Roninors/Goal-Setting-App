{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon --watch './**/*.ts' --exec 'ts-node' src/migrate.ts",
    "migration:generate": "drizzle-kit generate:pg --schema=./src/models/schema.ts",
    "migration:push": "node -r esbuild-register src/migrate.ts",
    "migrate": "drizzle-kit generate:pg --schema=./src/models/schema.ts && node -r esbuild-register src/migrate.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.20",
    "dotenv": "^16.3.1",
    "drizzle-orm": "^0.28.6",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/pg": "^8.10.7",
    "drizzle-kit": "^0.19.13"
  }
}
